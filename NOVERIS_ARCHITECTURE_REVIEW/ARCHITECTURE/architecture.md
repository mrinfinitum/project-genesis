# Architecture

Architecture version is 1.0.0.

Studio owns canonical definitions, contracts, content, metadata, verification, and publishing surfaces.

Game clients own rendering, local UI implementation, platform-specific code, camera, shaders, controls, and player runtime state.

Current architecture metrics:

- Sections: 31
- Decisions: 24
- Outstanding Decisions: 1
- Health Score: 100

