# Libraries

Library philosophy:

Cards -> Open Record -> Overview -> Children -> Assets -> Runtime -> History.

Libraries should browse generated canonical records only.

They should not show player progression, gameplay dashboards, unlock trees, research gates, or runtime monitors unless the page is explicitly a systems workspace.

