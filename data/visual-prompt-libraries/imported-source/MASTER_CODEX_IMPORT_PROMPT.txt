Import the included remaining NOVERIS Visual Prompt Library volumes one volume at a time. Preserve existing IA, IB, PA, and PB data. Validate every manifest, schema, checksum, ID, output reference, and record count. Add each volume to the Visual Production Engine, integrate applicable records into Creature Generator or Plant Generator, expose positive/negative/combined prompt copying, version-count and variation controls, canonical variable resolution, locked-field precedence, stale detection, prompt-pack exports, and runtime sanitization. Use a separate focused commit for every volume. Stop on conflicts; never overwrite approved canon silently.